name: Probe az CLI version (TEMPORARY -- Story 76.3, informational only, no Azure auth)

on:
  workflow_dispatch: {}

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Print az CLI version
        run: az version
